# 🏗️ Visualização da Arquitetura

## 📊 Diagrama de Componentes

```
┌─────────────────────────────────────────────────────────────┐
│                      index.html                              │
│                   (Ponto de Entrada)                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                ┌──────────▼───────────┐
                │   DOMContentLoaded   │
                │   Inicializa App     │
                └──────────┬───────────┘
                           │
        ┌──────────────────▼──────────────────┐
        │                                     │
        │         App (Facade)                │
        │   Orquestra componentes             │
        │                                     │
        │  - init()                           │
        │  - reload()                         │
        │  - getState()                       │
        │                                     │
        └──┬──────────────┬────────────┬──────┘
           │              │            │
    ┌──────▼─────┐   ┌────▼────┐  ┌────▼──────────┐
    │ DataLoader │   │ Chart   │  │  UIManager    │
    │            │   │ Builder │  │               │
    ├────────────┤   ├─────────┤  ├───────────────┤
    │ load()     │   │build()  │  │init()         │
    │getTasks()  │   │update() │  │handleFilter() │
    │transform() │   │export() │  │handleExport() │
    │filter()    │   │         │  │updateStats()  │
    └────────────┘   └─────────┘  └───────────────┘
         │                │              │
    cronograma.json   Plotly.js    HTML Elements
```

## 🔄 Fluxo de Dados

```
                    ┌─────────────────────┐
                    │   cronograma.json   │
                    │   (Dados Brutos)    │
                    └──────────┬──────────┘
                               │
                        ┌──────▼───────┐
                        │ DataLoader   │
                        │ - load()     │
                        │ - transform()│
                        └──────┬───────┘
                               │
                ┌──────────────┴──────────────┐
                │                             │
         ┌──────▼──────┐            ┌────────▼─────┐
         │ ChartBuilder│            │  UIManager   │
         │ - Format    │            │ - Events     │
         │ - Plotly    │            │ - Stats      │
         └──────┬──────┘            └────────┬─────┘
                │                             │
         ┌──────▼──────────────────┬──────────▼─────┐
         │                         │                │
      Gráfico Gantt          Estatísticas      Filtros
```

## 🎯 Responsabilidades por Módulo

```
┌─────────────────────────────────────────────────────────┐
│ App.js - ORQUESTRADOR                                   │
├─────────────────────────────────────────────────────────┤
│ ✓ Inicializa componentes                                │
│ ✓ Gerencia ciclo de vida                                │
│ ✓ Tratamento de erros global                            │
│ ✓ Dispara eventos customizados                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ DataLoader.js - DADOS                                   │
├─────────────────────────────────────────────────────────┤
│ ✓ Carrega JSON                                          │
│ ✓ Transforma dados                                      │
│ ✓ Filtra por critério                                   │
│ ✓ Acesso estruturado aos dados                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ChartBuilder.js - VISUALIZAÇÃO                          │
├─────────────────────────────────────────────────────────┤
│ ✓ Cria gráficos Gantt                                   │
│ ✓ Atualiza gráficos                                     │
│ ✓ Customiza cores e estilos                             │
│ ✓ Exporta como imagem                                   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ UIManager.js - INTERFACE                                │
├─────────────────────────────────────────────────────────┤
│ ✓ Cria controles                                        │
│ ✓ Gerencia eventos de usuário                           │
│ ✓ Mostra notificações                                   │
│ ✓ Atualiza estatísticas                                 │
└─────────────────────────────────────────────────────────┘
```

## 📈 Camadas da Aplicação

```
┌─────────────────────────────────────────────────────────┐
│                    APRESENTAÇÃO                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │ index.html - Estrutura HTML                      │  │
│  │ styles.css - Estilos CSS (Responsivo)            │  │
│  │ UIManager.js - Lógica de Apresentação            │  │
│  └──────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    APLICAÇÃO                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │ App.js - Orquestrador                            │  │
│  │ ChartBuilder.js - Lógica de Visualização         │  │
│  └──────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    DADOS                                │
│  ┌──────────────────────────────────────────────────┐  │
│  │ DataLoader.js - Acesso e Transformação de Dados  │  │
│  │ cronograma.json - Dados Persistentes             │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 🔗 Dependências Entre Módulos

```
index.html (ponto de entrada)
    ↓
Carrega: Plotly.js, DataLoader.js, ChartBuilder.js, UIManager.js, App.js
    ↓
App.js
    ├─→ DataLoader.js (para carregar dados)
    ├─→ ChartBuilder.js (para renderizar gráfico)
    └─→ UIManager.js (para interface)
```

## 🎨 Paleta de Cores

```
┌──────────────┬─────────────┬──────────────────────────┐
│ Cor          │ Valor Hex   │ Uso                      │
├──────────────┼─────────────┼──────────────────────────┤
│ Primária     │ #2196F3     │ Gráficos, botões, em andamento │
│ Sucesso      │ #4CAF50     │ Tarefas completadas      │
│ Erro         │ #f44336     │ Avisos, erros            │
│ Aviso        │ #ff9800     │ Notificações             │
│ Cinza Escuro │ #333333     │ Textos principais        │
│ Cinza Claro  │ #f5f5f5     │ Fundo                    │
└──────────────┴─────────────┴──────────────────────────┘
```

## 📱 Responsividade

```
Desktop (> 768px)          Tablet (768px - 480px)      Mobile (< 480px)
├─ 3 colunas               ├─ 2 colunas                ├─ 1 coluna
├─ Layout flex             ├─ Layout flex              ├─ Stack vertical
├─ Full gráfico            ├─ Gráfico adaptado         ├─ Gráfico comprimido
└─ Controles em linha      └─ Controles em coluna      └─ Controles empilhados
```

## 🚀 Fluxo de Inicialização

```
1. DOMContentLoaded
   ↓
2. Cria instância App
   ↓
3. await app.init()
   ├─ Cria DataLoader
   ├─ Cria ChartBuilder
   ├─ Cria UIManager
   ├─ Carrega dados
   ├─ Constrói gráfico
   └─ Inicializa UI
   ↓
4. Evento: app:initialized
   ↓
5. Aplicação pronta para uso
```

## 📊 Modelo de Dados (Task)

```
Task {
  id: number           - Identificador único
  name: string         - Nome da tarefa
  start: date         - Data de início (YYYY-MM-DD)
  end: date           - Data de término (YYYY-MM-DD)
  status: string      - Status (completed|in-progress|not-started)
  progress: number    - Percentual (0-100)
  responsible: string - Responsável
}
```

## 🔐 Encapsulamento e Privacidade

```
DataLoader
├─ Public:
│  ├─ constructor()
│  ├─ load()
│  ├─ getTasks()
│  ├─ getProject()
│  └─ transformForPlotly()
└─ Private (prefixo _):
   └─ (apenas métodos public)

ChartBuilder
├─ Public:
│  ├─ buildGanttChart()
│  ├─ updateChart()
│  └─ exportAsImage()
└─ Private (prefixo _):
   ├─ _createTrace()
   ├─ _createLayout()
   └─ _createConfig()

UIManager
├─ Public:
│  ├─ init()
│  └─ createControls()
└─ Private (prefixo _):
   └─ _handleExport()
   └─ _handleFilter()
```

## 🎯 Padrões de Design

| Padrão | Módulo | Benefício |
|--------|--------|-----------|
| Facade | App | Interface simples para componentes complexos |
| Separation of Concerns | Todos | Componentes independentes |
| Module Pattern | Todos | Encapsulamento |
| Observer | App | Comunicação desacoplada via eventos |
| Single Responsibility | Todos | Cada classe tem uma responsabilidade |

## 🔄 Estados da Aplicação

```
┌─────────────┐
│   Loading   │
└──────┬──────┘
       │
┌──────▼──────────┐
│  Initialized    │
└──────┬──────────┘
       │
┌──────▼──────────┐
│   Ready         │
└──────┬──────────┘
       │
   ┌───┴────────────────────────┐
   │                            │
┌──▼────────┐          ┌────────▼──┐
│  Filtering │          │ Exporting │
└───────────┘          └───────────┘
   │                            │
   └───────────┬────────────────┘
               │
        ┌──────▼──────┐
        │   Reloading │
        └─────────────┘
```

---

**Última Atualização**: Janeiro 2025  
**Versão**: 1.0
